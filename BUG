A)on peut toujours registerer une commande avec des arguments sans avoir de decorator shellMethod associé
	du coup le binder d'argument ne lui fourni aucun argument
	et on a une erreur du genre Unknown error: echo16() takes exactly 1 argument (0 given)
    
    modification a faire dans commande, à l'ajout de pre/pro/post process
    détail de la manière de gèrer ça dans engineCoreTest

B) dans les exception du shellMethod decorator, on ne donne pas le nom de la methode
	pas pratique s'il y a une erreur et qu'on vient d'ajouter plusieurs process

C) autocompletion bug
	ex: 
		1) write "ad"
		2) press tab
		3) press enter
		4) press up arrow
		5) press tab

		it will print "l" in place of "addon l"

D) prblm when autocompletion with var used in command name
    ex:
        1)var set toto add
        2)$toto
        3)press tab

    #QUICK solution: disable var interpretation in autocompletion

E) prblm avec l'autocompletion
    1) write "env g"
    2) press tab
    3) remove the get but leave the space char
    4) press tab again
    
    it will print "env environment"

F) autocompletion, missing cmd
    for example, echo and echo16
    echo16 is never print in the autocompletion list
        except with an empty line
        
G) si on met un environment parameter avec une value null dans le prototype de la fonction qui est decorée, 
   la variable engine dans le argchecker est à null
   
   Quelque chose comme ça ? tenter de reproduire le bug, car il y a des examples ou le bug ne se produit pas...
   
    @shellMethod(env=completeEnvironmentChecker())
    def toto(env = None):
   
H) "env add addonToLoad pcsc proxnroll" 
   ne rajoute pas les deux elements dans l'environment existant
   
I) autocompletion bug
    1)write "prox"
    2) TAB
    3)write "ca"
    4) TAB
    
    the "ca" is replaced with "a"
    
J) aucun message d'erreur n'est affiché quand certains arguments ne sont pas consommé


